
pub struct PartUser{
    pub name: String,
    pub provider_id: String,
    pub provider_type: String
}

pub struct User{
    pub id: u32,
    pub name: String,
    pub provider_id: String,
    pub provider_type: String
}
